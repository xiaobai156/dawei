# 大围数据_修复版2 — 项目规则

继承上级 `AGENTS.md`；本文件只记录本项目差异。未明确授权前只审计，不抓取、不写入、不删除。

## 数据契约

- 每站每期必须是 `01`—`49` 内恰好36个互不重复号码，保留网页原始顺序。
- `region` 是唯一方向字段，只能为 `top`/`bottom`；`position=tail` 仅兼容读取。
- `section_keywords`、`keywords` 必须在同一栏目和目标期上下文命中；不得跨栏目、跨文章、跨来源拼接。
- 三行数据只能使用 `three_rows` 或对应专属解析器；专属解析失败不得回退 `generic_36`。
- 动态文章的标题、作者、正文、36码、`record_id`、`record_path` 必须来自同一接口对象。

## 候选与窗口

- 页面候选超过30组或存在重复期号时，按方向使用最近5组高可信窗口；窗口不得运行时放宽。
- 指定期先做全部候选的同期冲突检查，再按窗口选期；越界、缺失、冲突或数量不符均失败。
- `search_window` 只是文本扫描边界，不是候选数量。
- 未指定期数的判重按各站方向确定最新候选，再以全站多数最新期生成近10期窗口，不能直接取全页最大期。

## 正式入口与文件

| 用途 | 入口 |
|---|---|
| 单期抓取 | `爬虫-每天大围网站数字.bat` → `scrape_all_36.py` |
| 多期抓取 | `爬虫-大围多期抓取不刷新缓存.bat` |
| 正式判重 | `爬虫-每天大围网站数字重复.bat` → `detect_duplicate_sites.py` |
| 失败站验证 | `validate_failed_sites_36.py` |

- `sites_36.json` 是唯一正式站点清单；`recent_10_cache.json` 是正式近10期缓存；`cache\` 仅作诊断，不能作成功数据来源。
- `V1_BASELINE_SHA256.txt` 是只读迁移基准，未经授权不得改写。
- 正式输出：`<期数>期-大围-成功.txt`、`<期数>期-大围-失败.txt`、`<期数>期重复检测失败.txt`。
- 多期全失败汇总使用 `多期-<期数列表>-大围-全部失败汇总.txt`；重复报告必须区分重复组/疑似组，并列出站名、URL、对齐期数、连续期数及每期号码位置和值。
- 多期入口不得更新正式近10期缓存；单期只能联网抓用户指定期，不得补抓相邻期。

## 新增、判重与缓存

- 新站正式加入前必须完成身份检查、真实来源确认、专属解析、方向窗口和判重。
- 候选站必须有缓存窗口内完整10期有效36码，并命中缓存最新期或上一期；普通“历史不足10期”特例不适用本项目。
- 正式缓存 `incomplete` 或有失败记录时，禁止新站判重入库；已有站不足10期可按共同期比较。
- 新站判重必须使用 `--use-backup --candidate-site` 和缓存副本，不能用全站实时抓取代替。
- 配置须保存 `site_id`、`source_type`、`parser_id`、`region`、`section_keywords`、`keywords` 及必要的动态字段；专属参数须有页面证据和测试支持。
- 缓存保留号码顺序、`record_id`、`source_path`、`raw_position`、`parser_id`；同期号码或来源身份冲突必须失败。
- 同期号码必须数值和位置都一致才算相同；双方都有 `record_id` 但ID不同，或无ID且来源路径不同，均不得自动覆盖。
- 单期先原子写成功/失败TXT，再更新缓存；缓存失败必须如实报告，不能改判结果。
- `--retry-failed`/`--only` 只处理指定站点：成功只追加到成功TXT末尾，失败只更新该站点该期，其他站点顺序、缓存和TXT保持不变。

## 严格范围与验收

- 新增、修复、验证和正式验收只能运行用户当次点名的站点、URL、期数、方向和字段，禁止全站。
- 成功必须证明站点身份、URL/文章ID、栏目锚点、字段语义、期数、方向、数量、合法值、来源区块、顺序和唯一性。
- 隔离验证只能写 `audit\`；前后核对正式配置、缓存和TXT未变。正式入口验收不能只调用解析函数或模拟正文。
- 受影响代码必须运行相关测试、Python 编译和静态检查；共享流程还需完整测试。未执行或未通过的检查不得宣称通过。

## 当前解析器

`generic_36`、`three_rows`、`xiongchumo`、`fenfatuqiang`、`xueqiu`、`baoma_xuanji`、`zhuchiren_weite`、`zhuchiren_baote`、`xiaoyuer`、`meirenyu`、`renjianrenai`、`fengkuang_zhongma`、`topic_content_3x12`、`marker_after_issue_3x12`、`section_3x12`、`yiyiba`、`kunnan_magazine`。

`kunnan_magazine` 缺少 `api_url` 直接失败；动态 `/article/admin/<记录ID>` 可能对应 `/api/proxy/manager-articles/<相同记录ID>`，禁止猜测或换文章。
